

export default function TodoList() {
    let todos = [
        'Hi',
        'Hello',
        'World'
    ]
    
    return (
        <ul>

            {todos.map((_todo, todwoIndex) => {
                return {
                    
                }
            })}
            
    
        </u>
    ) ;
}
